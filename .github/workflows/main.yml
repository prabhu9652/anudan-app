
name: Actions Workflow

on: [ push ]


jobs:
    run-github-actions: 
        runs-on: ubuntu-latest
        steps:
          - name: checkout source code from master
            uses: actions/prabhu9652/anudan-app@master
            run:
              ls
