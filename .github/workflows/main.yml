
name: Actions Workflow

on: [ push ]


jobs:
    run-github-actions: 
        runs-on: ubuntu-latest
        steps:
          - name: checkout 
            uses: actions/checkout@v1
            with:
               repository: prabhu9652/anudan-app
               path: anudan-app
            run:
              ls
